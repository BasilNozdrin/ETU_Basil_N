#include <stdio.h>

int main() {
    int ip[4], mask[4];
    scanf("%d.%d.%d.%d", ip, (ip+1), (ip+2), (ip+3));
    scanf("%d.%d.%d.%d", (mask+3), (mask+2), (mask+1), mask);
    
    for (int i = 0; i < 4; i++)
        if (ip[i] >> 8)
        {
            printf("wrong IP\n");
            return 0;
        }
    
    for (int i = 1; i <= 8; i++){
        unsigned long long int a = ((1<<8-1) << i);
        unsigned long long int b = (unsigned long long int) mask;
        if ((a & b != a)&&(a & b != b)){
            printf("wrong MASK\n");
            return 0;
        }
    }    
    
    for (int i = 0; i < 4; i++)
    {
        printf("%d", (*(ip+i))&(*(mask+(3-i))));
        if (i != 3)
            printf(".");
    }
    
    return 0;
}
